# Site Information
title: "LLmachine"
email: "kent.sew@gmail.com"
description: >-
  Welcome to LLmachine, your hub for AI, data science, and analytics.
  Explore projects, insights, and professional research.

baseurl: ""  # Ensure this is empty unless hosted under a subdirectory
url: "https://dataforsolution.github.io"  # Correct URL for GitHub Pages

# Theme and Plugins
remote_theme: jekyll/minima  # Ensures theme is applied correctly
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-paginate  # Ensures pagination works
  - jekyll-include-cache
  - jekyll-sitemap
  - jekyll-assets

# Pagination Settings
paginate: 5  
paginate_path: "/blog/page:num/"  # Changed to target blog pagination

# Markdown Processor
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false
  syntax_highlighter_opts:
    line_numbers: true

# Include and Exclude Paths
include:
  - _includes
  - _layouts
  - _projects
  - assets
  - assets/books
  - .nojekyll
  - favicon.ico
  - courses.html
  - dashboard.html
  - request-submitted.html
  - signin.html
  - signup.html
  - ebooks.md
  - _navigation.html
  - resources.html  # Ensure this file exists

exclude:
  - .sass-cache/
  - .jekyll-cache/
  - node_modules/
  - vendor/
  - Gemfile  # Keep if not needed for deployment
  - Gemfile.lock  # Keep if not needed for deployment

# Collections
collections:
  services:
    output: true
    permalink: /services/:path/
  projects:
    output: true
    permalink: /projects/:path/
  blog:
    output: true
    permalink: /blog/
  faq:
    output: true
    permalink: /faq/:path/
  contact:
    output: true
    permalink: /contact/
  signin:
    output: true
    permalink: /signin/
  signup:
    output: true
    permalink: /signup/
  resources:
    output: true
    permalink: /resources/:path/

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "LLmachine"
  - scope:
      path: "about"
    values:
      layout: "default"
      title: "About LLmachine"
  - scope:
      path: "services"
    values:
      layout: "default"
      title: "Our Services"
  - scope:
      path: "projects"
    values:
      layout: "default"
      title: "Our Projects"
  - scope:
      path: "signin"
    values:
      layout: "default"
      title: "Sign In"
  - scope:
      path: "signup"
    values:
      layout: "default"
      title: "Sign Up"
  - scope:
      path: "blog"
    values:
      layout: "default"
      title: "Blog"
  - scope:
      path: "resources"
    values:
      layout: "default"
      title: "Resources"

# Permalinks
permalink: pretty

# Sitemap Configuration
sitemap:
  exclude:
    - /404.html
  include:
    - /services/
    - /projects/
    - /blog/
    - /faq/
    - /contact/
    - /ebooks.md
    - /signin/
    - /signup/
    - /resources/  # Ensure this file exists

# SEO Defaults
jekyll_seo_tag:
  social:
    twitter:
      username: "SewLiben"
    github:
      username: "DataForSolution"

# Performance Optimization
cache: true
